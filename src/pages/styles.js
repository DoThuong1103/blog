import { styled } from "@mui/system";

export const IconAdd = styled("div")({
  position: "fixed",
  bottom: "2px",
  right: "16px",
});
export const Home = styled("div")({
  position: "relative",
});
